"""Auth: register / login / logout and the current_user dependency.

STUB -- filled in during the backend core phase (see docs/design/API.md § Auth).
"""
from fastapi import APIRouter

router = APIRouter()
