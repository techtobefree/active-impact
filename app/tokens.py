"""Impact-token ledger: mint/transfer primitives + ledger read + tip.

STUB -- filled in during the backend core phase. This module is the ONLY code
permitted to write token_entries or users.balance (see docs/design/DOMAIN.md).
"""
from fastapi import APIRouter

router = APIRouter()
