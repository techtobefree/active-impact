// STUB — filled in during the views build phase.
// Required exports (imported by app.js): meView, userView(username).
import { mount, el } from '../ui.js';
export async function meView() {
  mount(el('<div class="empty">Profile — coming soon in this build.</div>'));
}
export async function userView(_username) {
  mount(el('<div class="empty">Profile — coming soon in this build.</div>'));
}
