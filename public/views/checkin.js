// STUB — filled in during the views build phase.
// Required export (imported by app.js): checkinView(code).
import { mount, el } from '../ui.js';
export async function checkinView(_code) {
  mount(el('<div class="empty">Check-in — coming soon in this build.</div>'));
}
