// STUB — filled in during the views build phase.
// Required export (imported by app.js): walletView.
import { mount, el } from '../ui.js';
export async function walletView() {
  mount(el('<div class="empty">Wallet — coming soon in this build.</div>'));
}
