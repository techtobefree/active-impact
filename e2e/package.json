{
  "name": "active-impact-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright layer — drives the app as a user, screenshots + asserts each screen.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
